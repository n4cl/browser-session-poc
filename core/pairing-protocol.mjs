export const PAIRING_PROTOCOL_VERSION = 1;
export const PAIRING_SOCKET_MAX_MESSAGE_BYTES = 64 * 1024;
export const PAIRING_IDENTITY_FIELDS = Object.freeze([
  "session_id",
  "browser_instance_id",
  "profile_instance_id",
  "generation",
  "lease_id",
]);
